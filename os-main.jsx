import React from "react";
import ReactDOM from "react-dom/client";
import NithishOS from "./nithish-os.jsx";

ReactDOM.createRoot(document.getElementById("os-root")).render(
  <React.StrictMode>
    <NithishOS />
  </React.StrictMode>
);
